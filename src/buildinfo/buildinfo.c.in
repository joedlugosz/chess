/* build.c is generated by build.cmake from the template build.c.in */

#include "build.h"

/* Supplied by version.cmake */
const char git_version[] = "@git_version@";
const char os_name[] = "@os_name@";
const char target_name[] = "@target_name@";

const char build_date[] = __DATE__;

/* Supplied at compiler invocation - as quoted strings */
const char build_config[] = BUILD_CONFIG;
const char compiler_options[] = BUILD_FLAGS;

/* Compiler name and version */
#if defined(__clang__)
const char compiler_name[] = __VERSION__;
#elif defined(__GNUC__)
const char compiler_name[] = "gcc " __VERSION__;
#elif defined(_MSC_VER)
const char compiler_name[] = "MSVC " _MSC_VER;
#else
const char compiler_name[] = "Unknown";
#endif
